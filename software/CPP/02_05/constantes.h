#pragma once
#include <cmath>
#include <math.h>


const float epsilon0 = 8.854187 * pow(10, -12);
//const float mu0 = 4 * M_PI * pow(10, -7);
const float mu0 = 1.256637062*pow(10, -6);
const float Z0 = sqrt(mu0 / epsilon0);              //impédance du vide
const float c = 299792458;
